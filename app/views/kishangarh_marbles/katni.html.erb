<div class="col-sm-12">
  <div class="container">
    <div class="row">
      <div class="col-md-8" style="padding-top:15px;">
        <%= render "layouts/side_menu" %>
        <article>
          <h3>Katni Marble</h3>
          <div class="span7 popular_products">
          <ul class="thumbnails list-unstyled">
            <% @katni_marbles.each do |katni_marble| %>
              <li class="col-md-6">
                <div class="thumbnail">
                  <span><%= image_tag katni_marble.kishangarh_marble_pics.to_s, :width=>210,:height=>210 %></span>
                  <div class="productPicBoxText fonter" >
                    <span><%= katni_marble.name %></span>
                    <% if katni_marble.price.present? %>
                      <span style="float:right">Price <%= katni_marble.price %></span>
                    <% end %>
                  </div>
                </div>
                <% if current_admin %>
                  <div class="productPicBoxText fonter">
                    <span><%= link_to 'Edit',edit_kishangarh_marble_path(katni_marble) %> </span>
                  </div>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
        </article>
      </div>
      <div class="col-md-4">
        <div class="panel panel-primary">
          <div class="panel-heading">
            <h3 style="margin-top:0px;">Intrested?</h3><h3 class="panel-title">Get in Touch with the us</h3>
          </div>
          <%= render "layouts/query" %>
        </div>
      </div>
    </div>
  </div>
</div>