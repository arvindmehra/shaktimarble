<div class="col-sm-12">
  <div class="container">
    <div class="row">
      <div class="col-md-8" style="padding-top:15px;">
        <%= render "layouts/side_menu" %>
        <article>
          <div class="para">
            <h3>Dharmeta Marble</h3>
            <p>
              Dharmeta Marble is one of the oldest and finest quality produced in Dharmeta (a village), Rajasthan. It has milky white tone with green or brown shades with figurative patterns or designs on the surface. Dharmeta Marble is one of the most sought after qualitative product available in best competitive prices having abundant utility in residential & commercial purposes as a durable marble.
            </p>
          </div>
          <div class="span7 popular_products">
          <ul class="thumbnails list-unstyled">
            <% @dharmeta_marbles.each do |dharmeta_marble| %>
              <li class="col-md-6">
                <div class="thumbnail">
                  <div class="popup-gallery">
                    <span>
                      <%= link_to(image_tag(dharmeta_marble.kishangarh_marble_pics.to_s, :width=>210,:height=>210),dharmeta_marble.zoom_image.to_s, title: dharmeta_marble.name) %>
                    </span>
                  </div>
                  <div class="productPicBoxText fonter" >
                    <span><%= dharmeta_marble.name %></span>
                    <% if dharmeta_marble.price.present? %>
                      <span style="float:right">Price <%= dharmeta_marble.price %></span>
                    <% end %>
                  </div>
                </div>
                <% if current_admin %>
                  <div class="productPicBoxText fonter">
                    <span><%= link_to 'Edit',edit_kishangarh_marble_path(dharmeta_marble) %> </span>
                  </div>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
        </article>
      </div>
      <%= render 'layouts/query_area' %>
    </div>
  </div>
</div>